# ★ qwen-asr 는 의존성을 **정확히 고정**한다 — transformers==4.57.6 · accelerate==1.12.0,
# 그리고 [vllm] extra 는 vllm==0.14.0. 그래서 Dockerfile 의 베이스도 v0.14.0 으로 못박았다.
# 그러면 pip 가 vllm/torch 를 다시 깔지 않고(이미 충족) 나머지만 얹는다.
#
# ⚠️ 버전을 올릴 때는 **이 핀과 베이스 이미지 태그를 함께** 올려야 한다. 한쪽만 올리면
# pip 가 베이스의 CUDA 스택(nvidia-nccl 등)을 갈아엎는다 — 실측(2026-08-16): latest 베이스에
# qwen-asr 를 얹자 nvidia-nccl-cu13 2.30.7 이 제거되고 transformers 가 4.57.6 으로 내려갔다.
qwen-asr[vllm]==0.0.6

# fastapi·uvicorn·numpy 는 vllm 베이스에 이미 있으나, 베이스가 바뀌어도 서버가 뜨도록 명시한다.
fastapi
uvicorn[standard]
numpy
huggingface_hub[cli]

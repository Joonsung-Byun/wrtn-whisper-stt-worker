# qwen-asr 는 의존성을 정확히 고정한다 — transformers==4.57.6 · accelerate==1.12.0,
# [vllm] extra 는 vllm==0.14.0. 베이스가 CUDA 최소 이미지(venv 깨끗한 상태)라 pip 가
# 그 핀을 그대로 세우면 되고, 예전처럼 베이스에 이미 깔린 torch/vllm 과 충돌하지 않는다.
#
# ⚠️ 이력(2026-08-16): vllm/vllm-openai 베이스를 쓰던 시절에는 ① 베이스의 blinker 1.4 가
# distutils 설치라 pip 가 제거를 거부했고 ② 버전이 어긋나 pip 가 nvidia-nccl-cu13 까지
# 갈아엎었다. venv 베이스로 옮기면서 두 문제 모두 사라졌다.
qwen-asr[vllm]==0.0.6

fastapi
uvicorn[standard]
numpy
huggingface_hub[cli]
